---
name: add-provider
description: Ajouter un nouveau fournisseur de données météo au CLI.
license: MIT
---

# add-provider

1. Créer une classe dans `meteo/providers.py` qui implémente `Provider`.
2. L'enregistrer dans `PROVIDERS`.
3. Ajouter le nom dans l'aide du CLI (`meteo/cli.py`).

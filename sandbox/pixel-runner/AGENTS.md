# pixel-runner — instructions Codex

Jeu de plateforme 2D fictif en TypeScript. Écosystème Codex uniquement :
pas de CLAUDE.md ni de .claude/ ici.

- Le moteur vit dans `engine/`, les niveaux dans `levels/` (JSON).
- 60 FPS est un budget, pas un objectif : toute frame > 16 ms est un bug.
